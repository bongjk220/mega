# 🤖 mega 프로젝트 AI 에이전트 최우선 가이드 (AGENT.md)

이 문서는 **mega** 통합 저장소 내에서 작업하는 모든 AI 에이전트(Antigravity, Cursor, Windsurf, Claude Code 등)가 반드시 준수해야 하는 **최우선 행동 및 협업 지침**입니다.

---

## 📌 1. 저장소 구성 및 Context 개요

이 저장소는 다수의 서브 프로젝트와 마크다운 정보 파일로 이루어진 통합 저장소(Mega-Repo)입니다. 에이전트는 각 폴더의 목적에 맞게 독립적으로 행동해야 합니다.

*   `shop/` : 스프링 부트(Spring Boot) 백엔드, JPA/MyBatis, TiDB, Python RPA가 결합된 **스마트 식품 이커머스 서비스** (가장 엄격한 규칙 적용)
*   `goAI/` : AI 코딩 에이전트 트렌드 분석 및 도구 비교 정보가 있는 공간 (`AI종류.md`)
*   `goGit/` : Git 사용 가이드 공간 (`GIT처음시작.md`)
*   `demo/` : 데모 테스트 프로젝트
*   `.github/workflows/` : Render.com 서버 자동 배포 및 자가 핑(Keep-alive) Actions 설정 공간

---

## 🚨 2. 에이전트 핵심 행동 수칙 (Hard Rules)

### ① Permission First (선-승인 원칙)
*   파일의 생성, 수정, 삭제 작업을 진행하기 전 **반드시 사용자에게 변경 이유와 영역을 한국어로 친절하게 설명하고 명시적 동의(Approve)**를 구하십시오.
*   의존성(Dependency) 추가, 외부 라이브러리 설치 등 패키지 구조가 바뀌는 작업은 사전 승인이 필수입니다.

### ② Atomic Task (단일 작업 집중)
*   한 번의 태스크에서는 오직 하나의 기능 정의 또는 하나의 문제 해결에만 집중합니다.
*   동시다발적으로 여러 독립적인 영역을 한꺼번에 수정하지 마십시오.

### ③ Preserve Comments (주석 및 맥락 보존)
*   기존 코드에 포함되어 있는 기존 주석문은 단순 가독성을 위해 함부로 삭제해서는 안 됩니다.
*   기존의 맥락과 히스토리를 존중하고, 새로운 코드가 도입되었을 경우 **사용 이유(Reason)**, **실제 동작(Behavior)**, **출처(Source)**를 명확히 기술하십시오.

### ④ DB Safety Guardrails (데이터베이스 안전 보장)
*   `shop` 등 DB를 수반하는 작업 진행 시 데이터 무결성을 절대 해치지 마십시오.
*   대량 삭제/수정, 스키마 마이그레이션이 발생하는 쿼리는 반드시 사전에 사용자 검토를 마친 뒤 진행해야 합니다.
*   Safe Query Rule을 지키고, Hard Delete 대신 **Soft Delete(상태값 변경)**를 지향하십시오.

### ⑤ Credentials Security (보안 강화)
*   환경변수 `.env` 파일은 절대 Git에 커밋하지 않으며, API key나 DB credentials 정보를 코드에 하드코딩하지 않습니다.
*   `.env.example`을 템플릿으로 제공하고 실제 값은 로컬 환경변수로만 활용하십시오.

---

## 🛠️ 3. 서브 프로젝트별 준수사항

### 🛒 [shop] 식품 이커머스
*   **스택**: Spring Boot 4.0.5, Java, JPA, MyBatis, TiDB, Python RPA, JavaScript
*   **지침**: `shop/AGENTS.md` 및 `shop/CLAUDE.md`에 수록된 엄격한 가이드라인을 100% 준수해야 합니다.
*   **디자인**: 모든 프론트엔드 작업 시 `shop/docs/` 혹은 `shop/DESIGN.md`에서 규정한 컬러 팔레트, 타이포그래피, 마이크로 인터랙션을 그대로 유지하십시오.

### 💡 [goAI / goGit] 지식 축적 아카이브
*   최신 AI 도구에 대한 정보 및 팁을 주기적으로 업데이트하며, 가독성 높은 마크다운 형태를 유지합니다.

---

## 📝 4. 작업 이력 기록 (Work History)

*   작업이 완료되면 루트의 `WORK_HISTORY.md`에 다음과 같은 형식으로 히스토리를 명시적으로 기록합니다.
    ```markdown
    [시작시간 - 종료시간] 작업명: 작업 상세 요약
    ```
*   기록을 남김으로써 다음 에이전트 세션이나 사용자가 변경 이력을 추적할 수 있도록 돕습니다.
